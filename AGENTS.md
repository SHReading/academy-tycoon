# AGENTS.md

## 프로젝트

재수학원 3파전 경영 시뮬레이션 (가제: 학원 타이쿤). 브라우저 웹 빌드. TypeScript + Vite.
OpenAI Game Builders Seoul 2026 Track 1 온라인 예선 제출작. **마감 8/26.**

규칙의 단일 출처는 `인계문서/01_GAME_DESIGN.md`다. 코드와 문서가 다르면 **문서가 맞다.**

---

## 절대 규칙 (위반 시 해당 변경분 전체 폐기)

1. **실존 학원·강사·기업의 실명을 코드/콘텐츠/커밋 메시지/문서 어디에도 쓰지 않는다.**
   생성한 텍스트는 `npm run validate:content`의 실명 필터를 통과해야 한다.
2. **회사 내부 데이터·문서·수치를 반입하지 않는다.** 공개 정보와 일반 상식 수준만 사용한다.
3. `src/core/` 안에는 순수 함수만 둔다. **DOM · 브라우저 API · `Math.random()` · `Date.now()` 금지.**
   난수는 반드시 시드 RNG를 인자로 받는다. 같은 시드 + 같은 액션 = 항상 같은 결과.
4. **게임 규칙과 수치를 변경하지 않는다.** 문제를 발견하면 고치지 말고 **보고한다.**
   밸런스 판단 권한은 사람에게만 있다.
5. **런타임에 외부 API를 호출하지 않는다.** LLM API 호출도 포함. AI는 개발·콘텐츠 생산 단계에만 쓴다.
6. **의존성 추가 전 반드시 확인을 받는다.** 추가 시 gzip 번들 증가량을 PR에 적는다.
7. 스코프를 늘리지 않는다. 확장 아이디어는 구현하지 말고 `docs/PARKED.md`에 적는다.

---

## 작업 방식

- **자기 트랙 디렉터리 밖의 파일을 수정하지 않는다.** 트랙 경계는 아래와 같다.

  | 트랙 | 쓰기 가능 경로 |
  |---|---|
  | A. sim-core | `src/core/` |
  | B. ui | `src/ui/`, `index.html`, `src/styles/` |
  | C. content | `src/content/` |
  | D. harness | `src/harness/`, `scripts/` |

  트랙 경계를 넘어야 하는 변경은 코디네이터에게 올린다. 직접 고치지 않는다.

- 변경 후 아래가 **모두 통과**해야 커밋한다.
  ```
  npm run typecheck && npm run test && npm run validate:content
  ```
- 콘텐츠 생성물은 `npm run validate:content` 통과 전까지 머지하지 않는다.
- 타입은 `src/core/types.ts`에서만 정의하고 나머지는 import한다. 중복 선언 금지.

---

## 성능 예산 (초과 시 머지 불가)

| 항목 | 상한 |
|---|---|
| 초기 JS 번들 (gzip) | 200KB |
| 전체 초기 로드 | 1MB |
| 첫 인터랙션 가능 시점 | 1.5초 |
| 1턴 정산 계산 시간 | 16ms |

웹폰트 다운로드 금지. 시스템 폰트 스택만 쓴다. 스플래시·로딩 화면 금지.

---

## PR 설명 형식 (예외 없음)

모든 PR 본문에 아래 3줄을 넣는다. 이 기록이 쌓여 제출 항목 "Codex 활용 설명"이 된다.

```
사람이 결정한 것:
에이전트가 구현한 것:
해결한 문제:
```

번들 크기나 의존성이 변했다면 4번째 줄에 수치를 적는다.
