# PARKED — 이번 제출작에 넣지 않는 것

유혹이 올 때마다 여기를 본다. **지우지 말고 쌓는다.**
본선 진출 시 "확장 계획"을 말할 근거가 되고, 떨어져도 다음 버전의 백로그가 된다.

---

## 처음부터 스코프 밖 (00 문서 7절)

| 항목 | 이유 |
|---|---|
| 멀티플레이어 | 백엔드·매칭·동기화. 15일 안에 불가 |
| 계정 시스템 / 저장 기능 | 제출 요건이 "로그인 없이 즉시 실행" |
| 3D, 캐릭터 일러스트 | 아트 부담 0이 이 소재를 고른 이유 |
| 사운드 | 효과음 최소만. 현장 시연에서 스피커를 못 쓸 수도 있다 |
| 학생 개별 시뮬레이션 | 계산량과 UI 복잡도. 숫자 7개 원칙 위반 |
| 시간표 편성 | 반 편성과 딜레마가 겹친다. 하나면 충분 |
| 상세 재무제표 | 자금 한 줄이면 된다 |
| 튜토리얼 화면 | 규칙은 UI가 스스로 설명해야 한다 |
| 런타임 LLM 호출 | 비용·지연·가용성이 전부 리스크 (02 문서 1절) |

---

## 개발 중 밀어낸 것

여기에 추가할 때는 **날짜 / 아이디어 / 왜 뺐는지 / 넣는다면 어디에**를 적는다.

```
M/D — 아이디어
왜 뺐나:
넣는다면:
```

---

## 본선 이후 과제 (피치에서 "확장 계획"으로 언급 가능)

- **랭킹·리플레이 공유** — `core/telemetry.ts`의 `ScoreSink` 하나만 갈아끼우면 붙는다.
  시드 기반 결정론 코어라 리플레이는 시드+액션 시퀀스만 저장하면 된다. (02 문서 7절)
- **강사 성장·이탈** — 6턴은 성장을 체감하기엔 짧다. 장기전 모드에서.
- **콘텐츠 파이프라인 범용화** — 스키마 + 분포 조건 + 자동 검증 구조는 다른 카드 게임에 그대로 재사용 가능.
